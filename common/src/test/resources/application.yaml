spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://localhost:5432/book_change_db
    driver-class-name: org.postgresql.Driver
    username: root
    password: 123
  jpa:
    hibernate:
      ddl-auto: update